# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.lock

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Generated drift code (sẽ tự tạo lại khi chạy build_runner)
# Bỏ comment dòng dưới nếu KHÔNG muốn commit code generate sẵn
# lib/db/database.g.dart

# Platform-specific (sẽ tạo lại bằng `flutter create .`)
android/
ios/
macos/
windows/
linux/
# web/ KHÔNG ignore — vì cần giữ sqlite3.wasm + drift_worker.js
# đã setup thủ công theo hướng dẫn trong README (mục "Web")
